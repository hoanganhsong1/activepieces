@activepieces:registry=https://registry.npmjs.org/
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}  # disabled for local dev: NPM_TOKEN unset caused 401 on public pieces
legacy-peer-deps=true
save-exact=true
