Place your Motomura brand images here (filenames must match exactly):

  full-logo.png   - Horizontal logo with wordmark. Shown on login page and in emails.
                    Recommended: PNG, transparent bg, ~210px+ wide (e.g. 420x100 @2x).
  favicon.svg     - Browser tab icon. SVG preferred (or swap to .ico and update theme.ts).
  logo-icon.svg   - Small square mark for the sidebar/collapsed nav. SVG, square aspect.

These are served at https://motomura.io/branding/<file> and referenced from
packages/server/api/src/app/flags/theme.ts
